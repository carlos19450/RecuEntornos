package test.temperatura;

/**
 * Clase que convierto un numero 'decimal' a 'romano' y viceversa.
 *
 * @author Carlos ExpÃ³sito.
 */
public class RomDec {

    /**
     * MÃ©todo que conmvierte un numero romano en decimal.
     *
     * @param rom Variable de tipo String donde se guarda un numero romano.
     * @return Devuelve la conversiÃ³n de un numero romano a decimal.
     */
    public int roman2dec(String rom) {
        // Este metodo esta por implementar
        return 21;
    }

    /**
     * MÃ©todo que conmvierte un numero decimal en romano.
     *
     * @param dec Variable de tipo int donde se guarda un numero decimal.
     * @return Devuelve la conversiÃ³n de un numero decimal a romano.
     */
    public String dec2roman(int dec) {
        // Este metodo esta por implementar
        return "XXI";
    }
}
